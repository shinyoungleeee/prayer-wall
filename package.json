{
  "name": "prayer-wall",
  "version": "0.0.1",
  "repository": "git@github.com:shinyoungleeee/prayer-wall.git",
  "license": "MIT",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && lerna bootstrap",
    "clean": "lerna run clean && lerna clean -y && rm -rf",
    "dev": "lerna run dev --parallel",
    "lerna": "lerna"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
